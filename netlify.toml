[build]
  command   = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/ochel-foods run build"
  publish   = "artifacts/ochel-foods/dist/public"

[build.environment]
  NODE_VERSION   = "20"
  BASE_PATH      = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
